log_file = "log.json"

api_keys = dict(
    l2='+<9AkQNWb8_'
)

services_list = dict(
    redis='redis-server.service',
    ssh='ssh.service',
    openssh='ssh.service'
)

time_limit = 15
requests_limit = 3
